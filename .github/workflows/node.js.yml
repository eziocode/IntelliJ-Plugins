name: Node.js CI

on:
  push:
    branches: ["main"]
    paths:
      - "autodom-extension/server/**"
      - ".github/workflows/node.js.yml"
  pull_request:
    branches: ["main"]
    paths:
      - "autodom-extension/server/**"
      - ".github/workflows/node.js.yml"

jobs:
  build:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [18.x, 20.x, 22.x]

    defaults:
      run:
        working-directory: autodom-extension/server

    steps:
      - uses: actions/checkout@v4
      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: "npm"
          cache-dependency-path: autodom-extension/server/package-lock.json
      - run: npm ci
      - run: npm run build --if-present
      - name: Syntax check
        run: node -c index.js
